[pytest]
# Default: skip slow tests (anything marked @pytest.mark.slow).
# Run them explicitly with:  pytest -m slow
# Or run everything with:    pytest -m ""
addopts = -m "not slow"

markers =
    slow: stress / large-file tests; excluded from the default run
